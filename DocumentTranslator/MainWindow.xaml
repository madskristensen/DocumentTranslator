<Window x:Class="DocumentTranslator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DocumentTranslator"
        mc:Ignorable="d"
        Title="Document Translator"
        SizeToContent="Height"
        Width="720"
        MinHeight="500" MinWidth="560"
        Background="{DynamicResource WindowBackgroundBrush}"
        Foreground="{DynamicResource TextPrimaryBrush}"
        Icon="Assets/AppIcon.ico"
        AllowDrop="True"
        PreviewDragOver="Window_PreviewDragOver"
        Drop="Window_Drop"
        WindowStartupLocation="CenterScreen"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="ClearType"
        UseLayoutRounding="True">

    <Grid Margin="28,24,28,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                <TextBlock Text="Document Translator" Style="{StaticResource TitleTextStyle}"/>
                <TextBlock Text="Translate documents into multiple languages with Azure AI."
                           Style="{StaticResource SubtleTextStyle}"
                           Margin="0,4,0,0"/>
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <Button x:Name="ThemeButton"
                        Style="{StaticResource IconButtonStyle}"
                        ToolTip="Toggle theme (Light / Dark / System)"
                        Click="ThemeButton_Click"
                        Margin="0,0,4,0">
                    <TextBlock x:Name="ThemeIcon"
                               Text="&#xE706;"
                               FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets"
                               FontSize="18"/>
                </Button>
                <Button x:Name="SettingsButton"
                        Style="{StaticResource IconButtonStyle}"
                        ToolTip="Settings"
                        Click="SettingsButton_Click">
                    <TextBlock Text="&#xE713;"
                               FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets"
                               FontSize="18"/>
                </Button>
            </StackPanel>
        </Grid>

        <!-- Source document card -->
        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0,0,0,12">
            <StackPanel>
                <TextBlock Text="Source document" Style="{StaticResource FieldLabelStyle}"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="FilePathBox" Grid.Column="0" IsReadOnly="True"
                             Height="34" VerticalAlignment="Center"
                             Padding="10,4" VerticalContentAlignment="Center"
                             ToolTip="Drag and drop a file here, or click Browse"/>
                    <Button x:Name="BrowseButton" Grid.Column="1"
                            Content="Browse" MinWidth="100" Height="34"
                            VerticalAlignment="Center"
                            Margin="10,0,0,0" Click="BrowseButton_Click"/>
                </Grid>
                <TextBlock Style="{StaticResource SubtleTextStyle}"
                           Margin="0,8,0,0"
                           Text="Tip: you can also drag a document anywhere onto the window."/>
            </StackPanel>
        </Border>

        <!-- Source language card -->
        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Margin="0,0,0,12">
            <StackPanel>
                <TextBlock Style="{StaticResource FieldLabelStyle}">
                    <Run Text="Source language"/>
                    <Run Text=" (optional but recommended)" Foreground="{DynamicResource TextSecondaryBrush}" FontWeight="Normal"/>
                </TextBlock>
                <ComboBox x:Name="SourceLanguageBox"
                          DisplayMemberPath="DisplayName"
                          IsEditable="False"/>
            </StackPanel>
        </Border>

        <!-- Target languages card -->
        <Border Grid.Row="3" Style="{StaticResource CardStyle}" Margin="0,0,0,16">
            <StackPanel>
                <TextBlock Text="Target languages" Style="{StaticResource FieldLabelStyle}"/>

                <Grid>
                    <ToggleButton x:Name="TargetDropdownToggle"
                                  MinHeight="34"
                                  HorizontalContentAlignment="Stretch"
                                  Background="{DynamicResource ControlBackgroundBrush}"
                                  BorderBrush="{DynamicResource ControlBorderBrush}"
                                  BorderThickness="1"
                                  Foreground="{DynamicResource TextPrimaryBrush}"
                                  Cursor="Hand">
                        <ToggleButton.Template>
                            <ControlTemplate TargetType="ToggleButton">
                                <Border x:Name="Bd"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="6"
                                        TextElement.Foreground="{TemplateBinding Foreground}"
                                        SnapsToDevicePixels="True">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <ContentPresenter Grid.Column="0"
                                                          Margin="10,8"
                                                          VerticalAlignment="Center"
                                                          HorizontalAlignment="Left"/>
                                        <Path Grid.Column="1"
                                              Width="10" Height="6"
                                              Margin="0,0,12,0"
                                              VerticalAlignment="Center"
                                              Stretch="Uniform"
                                              Fill="{DynamicResource TextSecondaryBrush}"
                                              Data="M 0,0 L 10,0 L 5,6 Z"/>
                                    </Grid>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource ControlHoverBorderBrush}"/>
                                    </Trigger>
                                    <Trigger Property="IsChecked" Value="True">
                                        <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </ToggleButton.Template>
                        <TextBlock x:Name="TargetSummaryText"
                                   Text="Select languages..."
                                   Foreground="{DynamicResource TextTertiaryBrush}"
                                   TextTrimming="CharacterEllipsis"/>
                    </ToggleButton>

                    <Popup x:Name="TargetPopup"
                           PlacementTarget="{Binding ElementName=TargetDropdownToggle}"
                           Placement="Bottom"
                           StaysOpen="False"
                           AllowsTransparency="True"
                           PopupAnimation="Slide"
                           IsOpen="{Binding IsChecked, ElementName=TargetDropdownToggle, Mode=TwoWay}">
                        <Border Background="{DynamicResource CardBackgroundBrush}"
                                BorderBrush="{DynamicResource CardBorderBrush}"
                                BorderThickness="1"
                                CornerRadius="6"
                                MinWidth="{Binding ActualWidth, ElementName=TargetDropdownToggle}"
                                Margin="0,4,0,0">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="16" ShadowDepth="2" Opacity="0.25" Color="Black"/>
                            </Border.Effect>
                            <StackPanel>
                                <Grid Margin="8,8,8,4">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBox x:Name="TargetFilterBox" Grid.Column="0"
                                             Style="{StaticResource SearchTextBoxStyle}"
                                             Tag="Search languages..."
                                             TextChanged="TargetFilterBox_TextChanged"/>
                                    <Button Grid.Column="1" Content="Clear" Margin="6,0,0,0"
                                            Padding="10,4" MinHeight="0"
                                            Click="ClearTargetsButton_Click"/>
                                    <Button Grid.Column="2" Content="Done" Margin="6,0,0,0"
                                            Padding="10,4" MinHeight="0"
                                            Style="{StaticResource AccentButtonStyle}"
                                            Click="DoneTargetsButton_Click"/>
                                </Grid>
                                <ListBox x:Name="TargetLanguagesList"
                                         SelectionMode="Multiple"
                                         MaxHeight="320"
                                         BorderThickness="0"
                                         Background="Transparent"
                                         SelectionChanged="TargetLanguagesList_SelectionChanged">
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <Grid>
                                                <CheckBox IsChecked="{Binding IsSelected, RelativeSource={RelativeSource AncestorType={x:Type ListBoxItem}}}"
                                                          Content="{Binding DisplayName}"
                                                          VerticalAlignment="Center"
                                                          Foreground="{DynamicResource TextPrimaryBrush}"/>
                                            </Grid>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                    <ListBox.ItemContainerStyle>
                                        <Style TargetType="ListBoxItem" BasedOn="{StaticResource {x:Type ListBoxItem}}">
                                            <Setter Property="Padding" Value="8,4"/>
                                            <Style.Triggers>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter Property="Background" Value="Transparent"/>
                                                    <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </ListBox.ItemContainerStyle>
                                </ListBox>
                            </StackPanel>
                        </Border>
                    </Popup>
                </Grid>

                <TextBlock x:Name="TargetSelectionSubtext"
                           Style="{StaticResource SubtleTextStyle}"
                           Margin="0,8,0,0"
                           Text="No languages selected"
                           TextWrapping="Wrap"/>
            </StackPanel>
        </Border>

        <!-- Translate button -->
        <Button Grid.Row="4"
                x:Name="TranslateButton"
                Content="Translate"
                Style="{StaticResource AccentButtonStyle}"
                HorizontalAlignment="Right"
                MinWidth="140"
                Margin="0,0,0,16"
                Click="TranslateButton_Click"/>

        <!-- Progress card -->
        <Border Grid.Row="5" Style="{StaticResource CardStyle}">
            <StackPanel>
                <Grid Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Progress" Style="{StaticResource FieldLabelStyle}" Margin="0"/>
                    <TextBlock Grid.Column="1" x:Name="ProgressCountText"
                               Style="{StaticResource SubtleTextStyle}"
                               VerticalAlignment="Center"/>
                </Grid>
                <ProgressBar x:Name="OverallProgress"
                             Minimum="0" Maximum="1" Value="0"
                             Visibility="Collapsed"
                             Margin="0,0,0,10"/>
                <ListBox x:Name="ProgressList"
                         Height="140"
                         BorderThickness="0"
                         Background="Transparent"/>
            </StackPanel>
        </Border>
        </Grid>
</Window>
