<Window x:Class="DocumentTranslator.SettingsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings"
        SizeToContent="Height"
        Width="560" MinHeight="360"
        Background="{DynamicResource WindowBackgroundBrush}"
        Foreground="{DynamicResource TextPrimaryBrush}"
        Icon="Assets/AppIcon.ico"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="ClearType"
        UseLayoutRounding="True">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Azure Translator credentials"
                   FontSize="18" FontWeight="SemiBold"
                   Margin="0,0,0,16"/>

        <Border Grid.Row="1" Style="{StaticResource CardStyle}">
            <StackPanel>
                <TextBlock Text="Endpoint" Style="{StaticResource FieldLabelStyle}"/>
                <TextBox x:Name="EndpointBox"
                         ToolTip="e.g. https://your-resource.cognitiveservices.azure.com/"
                         Margin="0,0,0,14"/>

                <TextBlock Text="API key" Style="{StaticResource FieldLabelStyle}"/>
                <PasswordBox x:Name="KeyBox"/>
            </StackPanel>
        </Border>

        <TextBlock Grid.Row="2"
                   x:Name="StatusText"
                   Style="{StaticResource SubtleTextStyle}"
                   Margin="2,12,0,12"
                   TextWrapping="Wrap"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="TestButton" Content="Test connection"
                    MinWidth="140" Margin="0,0,8,0" Click="TestButton_Click"/>
            <Button x:Name="CancelButton" Content="Cancel"
                    MinWidth="90" IsCancel="True" Margin="0,0,8,0"/>
            <Button x:Name="SaveButton" Content="Save"
                    Style="{StaticResource AccentButtonStyle}"
                    MinWidth="90" IsDefault="True" Click="SaveButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
