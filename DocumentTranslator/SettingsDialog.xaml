<Window x:Class="DocumentTranslator.SettingsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings" Height="280" Width="520"
        Icon="Assets/AppIcon.ico"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Azure Translator endpoint" FontWeight="SemiBold"/>
        <TextBox Grid.Row="1" x:Name="EndpointBox" Margin="0,4,0,12"
                 ToolTip="e.g. https://your-resource.cognitiveservices.azure.com/"/>

        <TextBlock Grid.Row="2" Text="API key" FontWeight="SemiBold"/>
        <PasswordBox Grid.Row="3" x:Name="KeyBox" Margin="0,4,0,12"/>

        <TextBlock Grid.Row="5" x:Name="StatusText" Foreground="DimGray" Margin="0,0,0,8" TextWrapping="Wrap"/>

        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="TestButton" Content="Test connection" Width="140" Margin="0,0,8,0" Click="TestButton_Click"/>
            <Button x:Name="SaveButton" Content="Save" Width="90" IsDefault="True" Margin="0,0,8,0" Click="SaveButton_Click"/>
            <Button x:Name="CancelButton" Content="Cancel" Width="90" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
